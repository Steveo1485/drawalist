module IconHelper

  def gift_icon(options = {})
    icon('gift', options)
  end

  def group_icon(options = {})
    icon('users', options)
  end

  def list_icon(options = {})
    icon('list', options)
  end

  def star_icon(options = {})
    icon('star', options)
  end

  def tree_icon(options = {})
    icon('tree', options)
  end

end