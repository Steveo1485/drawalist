.row.group
  .col-sm-12
    %h2= group.decorate.display_name
    = link_to('Group Details', group, class: 'group-details')
  .col-sm-12.col-md-6
    = render current_user.list_for_group(group)
  .col-sm-12.col-md-6
    = render 'lists/paired_list', list: current_user.paired_list_for_group(group)